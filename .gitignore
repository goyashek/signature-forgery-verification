# Raw NFI download — 189 MB, heavily duplicated, and 205 images carry inconsistent
# filename IDs. NOT committed. The clean, deduplicated, resized subset lives in
# sign_data_nfi/ (tracked) — see sign_data_nfi/README.md.
sign_data2/

# Personal tooling, not part of the project
2.md
1.md

# Saved model artifacts (regenerated by the notebooks)
models/*.keras
models/*.json
models/*.h5

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store

signature-verification-using-rnn-c-rnn-b-rnn.ipynb

# raw downloaded datasets (BHSig260 etc.) — derived into sign_data_combined/
archive/

# Local research log (working notes, not part of the public repo)
research.md
